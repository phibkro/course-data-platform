# ADR-003: Base UI as the default primitive layer

## Status

Accepted for the first vertical slices.

## Decision

Use Base UI/shadcn-compatible primitives for ordinary controls and overlays. Evaluate React Aria for complex tables, grid lists, and collection semantics. Do not combine multiple focus-management systems inside one interaction tree.
